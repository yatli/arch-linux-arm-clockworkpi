# Maintainer: yatli <yatao.li -at- live.com>
pkgname=wiringpi-clockworkpi-cm4-bin
pkgver=0.1
pkgrel=1
pkgdesc="Arduino wiring-like library written in C for RaspberryPi."
url="http://wiringpi.com/"
arch=('aarch64')
license=('LGPL3')
makedepends=('git')
conflicts=('wiringpi')
source=('wiringpi-clockworkpi-cm4-bin.tar.gz')
md5sums=('SKIP')

package() {
	tar xf "${srcdir}/wiringpi-clockworkpi-cm4-bin.tar.gz" -C "${pkgdir}"
}
